# Test Sign-Off

---

## Current Status: 🟢 Rounds 2 through 6 of the prose-refresh story are all merged, deployed to staging, and QA-signed-off. Round 6 (hero sub-paragraph clause reorder + bio-paragraph opener trim, both in `public/index.html`) merged to `main` as PR #15 (squash commit `270558e83b6062ada141a19a3bb5633cd9d7efc9`), auto-deployed to staging (workflow run `34795666261`), and independently re-validated by QA against live staging — 74/74 pass. Staging Definition-of-Done gate is now GRANTED for round 6. The pre-edit wording this round replaces is currently live in production (`v0.1.6`); the human wants this shipped to production immediately now that staging is green. No tag pushed, no prod deploy triggered yet — that is the next, separate, PM/human-owned step. See Sign-Off History below.

Unit tests exist (`src/App.test.js`) and run in CI via `npm test`. Full Playwright E2E suite (59 tests across about/contact/homepage/navigation/projects) independently validated locally against `npx serve public/` for PR #8 (content/refresh-projects-and-homepage) on 2026-09-08 — 59/59 pass. `e2e/about.spec.js` + `e2e/projects.spec.js` (31 tests) independently re-validated the same way for PR #11 (content/project-portfolio-followup) on 2026-09-12 — 31/31 pass (pre-merge, against a local static server). PR #11 subsequently merged to `main` (squash commit `9c76ac1`) and auto-deployed to staging; the same 31 tests were re-run against the live staging URL on 2026-09-12 as the Stage 5 Definition-of-Done gate — 31/31 pass. A follow-up round on `content/tagline-tweak` (hero tagline + 3 highlight-bullet rewords) was independently re-validated pre-merge on 2026-09-12 — 58/58 pass across the full `homepage`/`about`/`projects`/`contact` suite. That round subsequently merged to `main` as PR #12 (squash commit `6538352`) and auto-deployed to staging; the same 58 tests were re-run against the live staging URL on 2026-09-12 as the Stage 5 Definition-of-Done gate — 58/58 pass. A fourth round on new branch `content/prose-round-4` (off `main` @ `cd03314`, which already contains PR #11 and PR #12) covers 5 more copy edits to `public/index.html` (hero sub-paragraph, bio-paragraph opener, banking-bullet rewording, delivery & leadership skills cell, tagline capitalization) — TDD-gate tests written and confirmed red 2026-09-12 (5 failing / 57 passing), ahead of Frontend implementation. Frontend Engineer then implemented all 5 edits directly in `public/index.html`; QA independently re-validated against the implemented working tree on 2026-09-12 — 62/62 pass. See Sign-Off History for details.

### What exists
- Unit tests for `About` page (content assertions)
- Unit tests for `Projects` page (card count, project names, repo links)
- Tests run automatically in CI (`react-scripts test` via GitHub Actions — if wired; currently not in deploy.yml)

### What is missing
- No test run in the CI/CD deploy pipeline (tests are not run before deploy in `.github/workflows/deploy.yml`)
- No browser/E2E tests
- No formal QA sign-off for any production deployment

---

## Sign-Off History

| Date | Environment | QA Agent | Scope | Result | Notes |
|------|-------------|----------|-------|--------|-------|
| 2026-04-18 | Unit/E2E | Application Engineer | Projects page — 5-card count, new card content, Spotify link | Pass | Unit tests pass locally; E2E verified against spec |
| 2026-09-08 | Local E2E, pre-merge (PR #8) | QA Engineer | Story: projects/homepage/contact content refresh (`content/refresh-projects-and-homepage` branch, commit `3dc60ed`) — independent validation of QA-03/04/05 target-state tests against implemented branch, requested by Code Reviewer because prior pass evidence was Frontend Engineer self-report only | Pass — 59/59 | Ran full Playwright suite (`e2e/about.spec.js`, `e2e/contact.spec.js`, `e2e/homepage.spec.js`, `e2e/navigation.spec.js`, `e2e/projects.spec.js`) via `npx playwright test` with `PLAYWRIGHT_BASE_URL=http://localhost:4173` against `npx serve public -l 4173` (not `python -m http.server` on `build/`, which does not replicate clean-URL routing for `/projects`, `/contact`); confirmed `/`, `/projects`, `/contact` all return 200 before running suite. Independently reproduces Frontend Engineer's reported 59/59. No files modified other than this entry. |
| 2026-09-12 | N/A — TDD gate, write-tests-first pass (not a live-validation sign-off) | QA Engineer | About-page copy refresh on `content/project-portfolio-followup` (not yet implemented): lede sentence, bio paragraph, highlights-list merge (8→7 items) + 3 bullet rewrites, and 3 skills-grid cell edits, all in `public/index.html`, ahead of Frontend Engineer implementation | Red confirmed (as intended) — 8 failing / 7 passing in `e2e/about.spec.js` | Updated `e2e/about.spec.js` to assert the NEW copy before it exists, per TDD gate. Replaced the `53%`/`77%`/`1,000+` metrics assertion (all three numbers are changing/being removed) with one asserting `70%`, `Sev 1`, `MTTR`, `countless hours`. Added 7 new assertions covering the remaining planned edits not previously pinned by any test: lede value-framing, bio ecosystem/coaching language, the banking-bullet merge (asserts exactly 1 `li` containing both the 5M+ stat and the "If it broke" clause), the AWS DevOps Agent bullet replacing the GPT/Claude/MCP bullet, the "Led over 10 migrations" bullet, `Kiro` appearing in the ai & automation skills cell, and "observability strategy" moving from the observability cell to the delivery cell. Ran `npx playwright test e2e/about.spec.js` locally against live `https://ericreilly.com` (default `playwright.config.js` baseURL, read-only GETs, no CI triggered): 8 failed exactly as expected (all tied to not-yet-shipped copy) / 7 passed (pre-existing assertions unaffected by this content round — highlights count ≥5, 3 employer rows, job titles, 4-cell grid count, generic ai/automation + observability regex, "7+ years" — the last two are unaffected because the words themselves persist in unrelated locations). Also ran `e2e/projects.spec.js` (16/16 pass, confirmed untouched by this change) and inspected `src/App.test.js`: it asserts against the legacy, unbuilt `src/pages/About.js` component (still contains "53%"), which is explicitly out of scope for this pass per task boundaries — left unchanged since that component is not being edited. No production content (`public/index.html`) or legacy `src/pages/About.js` modified. No PR opened, no tag pushed, no CI triggered. Next: Frontend Engineer implements the 4 copy changes; QA re-runs this same suite to confirm green before sign-off. |
| 2026-09-12 | Local E2E, pre-merge (PR #11) | QA Engineer | **Independent QA validation pass** (distinct from this same date's red/TDD-gate entry above) — re-run of `e2e/about.spec.js` and `e2e/projects.spec.js` against the *implemented* state on `content/project-portfolio-followup` (commit `8c0d8cb`, content) / `5f35da0` (branch tip at time of run), requested by Code Reviewer on PR #11 because the "15/15 / 16/16 passing" test-plan checklist in the PR body was self-reported by the same Frontend/QA-write-tests session that authored the implementation, with no independent corroboration — same failure mode the PR #8 precedent (row above, 2026-09-08) exists to guard against | Pass — 31/31 (15/15 `about.spec.js` + 16/16 `projects.spec.js`) | Started `npx serve public -l 4173` (matches the PR #8 precedent method — not `python -m http.server` on `build/`, which does not replicate production's clean-URL rewrite for extensionless `/projects`, `/contact`); confirmed `/`, `/projects`, `/contact` all returned 200 before running the suite. Ran `PLAYWRIGHT_BASE_URL=http://localhost:4173 npx playwright test e2e/about.spec.js e2e/projects.spec.js --reporter=list`: 31/31 passed in 3.4s, 0 failures, 0 flakes/retries. Did not point the suite at production (`https://ericreilly.com`, the config's default `baseURL`) since nothing from this branch has been deployed there yet — that would only re-test stale live content, not this branch's changes. Server was stopped after the run; no files modified other than this entry (`public/index.html`, `e2e/*.spec.js` untouched). This satisfies the PR #8 precedent Code Reviewer cited: an independent QA re-run against the implemented branch, corroborating (not merely repeating) the Frontend/QA self-report, recorded here and posted as a PR #11 comment. No PR opened, no tag pushed, no deploy triggered, PR not merged. |
| 2026-09-12 | Live staging (`https://staging.ericreilly.com`) — Stage 5 Definition-of-Done gate | QA Engineer | **Third QA touch on this story** (distinct from the same day's TDD-gate red pass and independent pre-merge local-green pass above) — full live-environment validation of PR #11 after merge and auto-deploy. PR #11 merged to `main` as squash commit `9c76ac1`; the `Deploy Website` workflow (run `34702470524`, auto-triggered on push to `main`) completed with `deploy-staging`, `deploy-prompted-staging`, and `e2e-staging` (CI's own copy of this gate, running the identical suite/config) all green. DevOps spot-checked `https://staging.ericreilly.com/` via curl and confirmed the new copy (new lede, merged banking bullet, "Reduced Sev 1 incidents by over 70%", new "Claude Code, Codex, Kiro, Python, Bash" skills line) is live. This pass is the independent local re-run of that same gate ahead of human approval, per Definition of Done (staging E2E green + this report) | Pass — 31/31 (15/15 `about.spec.js` + 16/16 `projects.spec.js`) | Checked out clean `main` at `9c76ac1` (stashed unrelated local WIP edits to `.project/decisions.md`, `.project/deployment-log.md`, `.project/workflow-state.md` first — pre-existing uncommitted changes from an earlier PM session, unrelated to this validation, left untouched in the stash). Confirmed `https://staging.ericreilly.com/` and `https://staging.ericreilly.com/projects` both returned `200` via curl before running the suite. Ran `PLAYWRIGHT_BASE_URL=https://staging.ericreilly.com npx playwright test e2e/about.spec.js e2e/projects.spec.js --reporter=list` locally (never via the CI live-E2E workflow — `e2e-staging` already ran once as part of the deploy and is a final safety net, not an iteration/validation tool per CI-minute discipline): 31/31 passed in 4.6s, 0 failures, 0 flakes/retries, 0 retries configured. Verified test-suite hygiene before running: no `page.route()` or network interception in `e2e/about.spec.js` / `e2e/projects.spec.js` (RULE-1 — confirmed via grep, no matches); no auth flow on this static site (RULE-4 N/A); all 31 assertions are read-only GETs against rendered content with no data creation, so no cleanup was required (RULE-5 N/A); this project's convention (established across the 2026-09-08 and 2026-09-12 entries above, and mirrored by CI's own `e2e-staging` job) is a single `playwright.config.js` driven by the `PLAYWRIGHT_BASE_URL` env var rather than a separate live config file or `CI_LIVE_E2E` gate variable — noted as an accepted project-specific deviation from the generic live-test-gate pattern, not a gap, since there is no environment mutation risk in a read-only content suite. No files modified other than this entry; `public/*.html` and `e2e/*.spec.js` untouched. No tag pushed, no production deploy triggered, no CI live-E2E workflow re-run. QA sign-off GRANTED for staging; production promotion (tag push) is a separate, not-yet-requested step. |
| 2026-09-12 | N/A — TDD gate, write-tests-first pass (not a live-validation sign-off) | QA Engineer | Homepage hero-tagline tweak on new branch `content/tagline-tweak` (off `main` @ `92f2458`, which already contains PR #11): drop "and doesn't break" from `public/index.html`'s `h1.headline` so the tagline reads "i build stuff that scales" instead of "i build stuff that scales and doesn't break." — not yet implemented; `public/index.html` untouched by this pass | Red confirmed (as intended) — 1 failing / 10 passing in `e2e/homepage.spec.js` | Searched `e2e/about.spec.js` and `e2e/projects.spec.js` first per task framing but found no assertion referencing this phrase — the line lives in the homepage hero (`public/index.html` line 37, inside `<header class="hero reveal"><h1 class="headline">`), so the actually-relevant file is `e2e/homepage.spec.js`, which did have coverage: a loosened regex `expect(text).toMatch(/break\|reliab/i)` on the `h1.headline` text, added 2026-09-07 (see `.project/decisions.md` 2026-09-07 #2) specifically so the hero copy could keep changing during the voice-rewrite story without forcing test rewrites. That regex would start failing for the wrong reason once "break" is removed and "reliab" was never in the `h1` (only in the `.sub` paragraph) — so retired it in favor of (a) a plain structural test (`headline` visible, has `.token` spans, non-empty text) and (b) a new dedicated test, `hero tagline drops the "and doesn't break" clause`, asserting `toContainText('i build stuff that scales')` and `not.toContainText("and doesn't break")`, mirroring the new-text-present/old-text-absent pattern used for the about-page bio/lede assertions added 2026-09-12 (commit `7cc59cb`). Confirmed this tagline line predates PR #11 entirely (`git blame` → commit `a433ca2`, 2026-04-18), so it is unaffected by the prod/staging PR #11 gap and safe to validate against live production. Ran `npx playwright test e2e/homepage.spec.js --reporter=list` against live `https://ericreilly.com` (default `playwright.config.js` baseURL, read-only GETs, no CI triggered): confirmed `200` via curl first, then 1 failed exactly as expected (the new dedicated tagline test — current live copy still contains "and doesn't break") / 10 passed, including the restructured structural headline test, confirming the regex retirement introduced no regression. No production content (`public/index.html`) modified. No PR opened, no tag pushed, no CI triggered. Next: Frontend Engineer drops "and doesn't break" from `public/index.html`; QA re-runs `e2e/homepage.spec.js` to confirm green before sign-off. |
| 2026-09-12 | N/A — TDD gate, write-tests-first pass (not a live-validation sign-off) | QA Engineer | **Same round, expanded scope** (PM bundled 3 more `.highlights li` bullet rewords into the same `content/tagline-tweak` branch, landing in `public/index.html` alongside the tagline drop): (1) banking bullet — "Platform owner for an international core banking SaaS on Azure that grew from 0 → 5M+ accounts in five years. If it broke, it was my problem." → "Owned an international core banking SaaS on Azure that grew from 0 to 5M+ accounts in five years." (drops the "if it broke" clause, arrow → "to"); (2) AI-rollout bullet — "Rolled out AWS DevOps Agent across the portfolio to bolster incident response and troubleshooting, provisioned with Terraform and integrated with Datadog." → "Put AWS DevOps Agent to work across the whole portfolio. Faster incident response, sharper troubleshooting, wired straight into Terraform and Datadog."; (3) migrations bullet — "Led over 10 migrations in various forms: on-prem infra, containerization, cloud infrastructure, and data." → "Led 10+ migrations spanning on-prem infra, containerization, cloud infrastructure, and data." All three confirmed present verbatim in current `public/index.html` (lines 85, 87, 88) before editing tests; none yet implemented | Red confirmed (as intended) — 4 failing / 54 passing across `e2e/about.spec.js`, `e2e/homepage.spec.js`, `e2e/projects.spec.js`, `e2e/contact.spec.js` | Updated 3 assertions in `e2e/about.spec.js`: (1) `banking platform highlight is merged into a single bullet...` renamed to `...drops the "if it broke" clause and uses "0 to 5M+" wording` — filter changed from `hasText: '0 → 5M+ accounts'` to `hasText: '0 to 5M+ accounts'`, added `toContainText('Owned an international core banking SaaS')`, replaced the old `toContainText('If it broke, it was my problem')` with two `not.toContainText` checks (old clause, old arrow wording) since the round removed that clause entirely; (2) added a new test `AI rollout highlight uses the new "put to work" phrasing...` alongside the still-valid existing "AWS DevOps Agent present / MCP bullet absent" test — confirmed the existing test does NOT pin any now-dead wording (it only checks the `AWS DevOps Agent` substring and the unrelated old `MCP enablement for the team` phrase, both still true after the reword) so it needed no change, but added the new test to pin the actually-changing substring (`wired straight into Terraform and Datadog` present, `provisioned with Terraform and integrated with Datadog` absent) per this repo's convention of covering intentionally-changing prose lines; (3) `migrations highlight...` — changed regex from `/Led over 10 migrations/` to `/Led 10\+ migrations/` and added a `not.toHaveCount(0)` check on the old phrase. Ran the full suite (`about`, `homepage`, `projects`, `contact` specs) locally against `npx serve public -l 4173` (not production — production is still pre-PR-11 and would show unrelated pre-existing failures for the whole about-page section; `localhost:4173` serves the current branch's content, which is `main`'s PR-11 state, giving a clean signal isolated to only this round's 4 changes): confirmed `/`, `/projects`, `/contact` all returned `200` first, then `PLAYWRIGHT_BASE_URL=http://localhost:4173 npx playwright test e2e/about.spec.js e2e/homepage.spec.js e2e/projects.spec.js e2e/contact.spec.js --reporter=list`: exactly 4 failed (the 3 new/updated highlight assertions plus the previously-recorded tagline test) / 54 passed, confirming no regressions and that all 4 red failures are wired to the correct, currently-unshipped copy. Also answered a PM question on em-dash coverage scope: `no em dash appears anywhere in body text` (checking literal `—`, U+2014) exists in `e2e/homepage.spec.js`, `e2e/projects.spec.js`, and `e2e/contact.spec.js` but not as a separate test in `e2e/about.spec.js` — not a gap, because `about.spec.js`'s `beforeEach` also hits `/` (about content lives on the same single page as the homepage hero, not a separate route), so `homepage.spec.js`'s em-dash check already covers the about/highlights section's body text; that check passed (0/58 tests touching em dash content flagged anything) confirming the "0 → 5M+ accounts" bullet's arrow character (U+2192, rightwards arrow) is a distinct glyph from em dash and was never in scope of that rule, and bullet 2's new period-based phrasing doesn't change that. No production content (`public/index.html`) modified. No PR opened, no tag pushed, no CI triggered. Committed to `content/tagline-tweak` as commit `4b10603`. Next: Frontend Engineer implements all four copy changes (tagline + 3 highlight bullets) together in `public/index.html`; QA re-runs this same suite to confirm green before sign-off. |
| 2026-09-12 | Local E2E, pre-merge (branch `content/tagline-tweak`) | QA Engineer | **Independent QA validation pass** (distinct from this same date's two TDD-gate red entries above) — re-run of the full suite against the *implemented* working-tree state on `content/tagline-tweak` (Frontend Engineer's edits to `public/index.html` were uncommitted at the time of this run), requested by PM per the PR #11 precedent (Code Reviewer previously blocked a "tests pass" claim that was self-reported by the same agents who wrote the code, with no independent corroboration). Covers all four copy changes in one working tree: hero tagline drops "and doesn't break"; banking-platform bullet drops "If it broke, it was my problem" and switches "0 → 5M+" to "0 to 5M+"; AI-rollout bullet reworded to "...wired straight into Terraform and Datadog"; migrations bullet changed to "Led 10+ migrations spanning..." | Pass — 58/58 (`e2e/homepage.spec.js`, `e2e/about.spec.js`, `e2e/projects.spec.js`, `e2e/contact.spec.js`) | Diffed `public/index.html` against the previous commit first and confirmed all four edits matched the planned copy verbatim (no drift, no extra changes) before running tests. Started a fresh `npx serve public -l 4173` (matches this story's established local-validation method); confirmed `/`, `/projects`, `/contact` all returned `200` via curl before running the suite. Ran `PLAYWRIGHT_BASE_URL=http://localhost:4173 npx playwright test e2e/homepage.spec.js e2e/about.spec.js e2e/projects.spec.js e2e/contact.spec.js --reporter=list`: 58/58 passed in 4.7s, 0 failures, 0 flakes/retries — independently reproduces Frontend Engineer's reported 58/58, including the 4 assertions that were red in the two TDD-gate entries above (tagline, banking bullet, AI-rollout bullet, migrations bullet), now green. Server stopped after the run. This satisfies the PR #11 precedent Code Reviewer cited: an independent QA re-run against the implemented working tree, corroborating (not merely repeating) the Frontend self-report. `public/index.html` and `e2e/*.spec.js` not modified by this entry (test files were already committed in the two prior TDD-gate commits `409b3be` / `4b10603`; only `public/index.html` was pending commit, which QA committed alongside this sign-off entry per PM instruction — see commit history). No tag pushed, no deploy triggered. QA sign-off GRANTED for this working-tree state; PR opened against `main` for Code Reviewer per PM instruction. |
| 2026-09-12 | Live staging (`https://staging.ericreilly.com`) — Stage 5 Definition-of-Done gate | QA Engineer | **Round 2 of this gate on the same story class as PR #11** — full live-environment validation of PR #12 (hero tagline shortened + 3 highlight-bullet rewords) after merge and auto-deploy. PR #12 merged to `main` as squash commit `6538352a0ee7fa19bfbb447394323fc1e44f80ea`; the `Deploy Website` workflow (run `34704340442`, auto-triggered on push to `main`) completed with `deploy-staging`, `deploy-prompted-staging`, and `e2e-staging` (CI's own copy of this gate) all green. DevOps spot-checked `https://staging.ericreilly.com` via curl and confirmed the new copy (tagline, 3 bullets) is live there. This pass is the independent local re-run of that same gate ahead of human approval, per Definition of Done (staging E2E green + this report) | Pass — 58/58 (`e2e/homepage.spec.js`, `e2e/about.spec.js`, `e2e/projects.spec.js`, `e2e/contact.spec.js`) | Checked out clean `main` at `6538352` (stashed pre-existing unrelated uncommitted WIP to `.project/decisions.md`, `.project/deployment-log.md`, `.project/project-status.md`, `.project/workflow-state.md`, and untracked `lessons-learned.md` first — same pre-existing-WIP pattern noted in the PR #11 staging entry above, unrelated to this validation, restored afterward via `git stash pop` once back on `content/tagline-tweak`). Confirmed `https://staging.ericreilly.com/`, `/projects`, and `/contact` all returned `200` via curl before running the suite. Ran `PLAYWRIGHT_BASE_URL=https://staging.ericreilly.com npx playwright test e2e/homepage.spec.js e2e/about.spec.js e2e/projects.spec.js e2e/contact.spec.js --reporter=list` locally (never via the CI live-E2E workflow — `e2e-staging` already ran once as part of the deploy and is a final safety net, not an iteration/validation tool per CI-minute discipline): 58/58 passed in 4.8s, 0 failures, 0 flakes/retries, 0 retries configured. Verified test-suite hygiene before running: no `page.route()` or network interception anywhere under `e2e/` (RULE-1 — confirmed via grep, no matches); no auth flow on this static site (RULE-4 N/A); all 58 assertions are read-only GETs against rendered content with no data creation, so no cleanup was required (RULE-5 N/A); this project continues its established convention of a single `playwright.config.js` driven by `PLAYWRIGHT_BASE_URL` rather than a separate live config file or `CI_LIVE_E2E` gate variable (accepted deviation, mirrored by CI's own `e2e-staging` job, not a gap — no environment-mutation risk in a read-only content suite). No files modified other than this entry; `public/*.html` and `e2e/*.spec.js` untouched; no test files edited. No tag pushed, no production deploy triggered, no CI live-E2E workflow re-run. QA sign-off GRANTED for staging; production promotion (tag push) is a separate, not-yet-requested step. |
| 2026-09-12 | N/A — TDD gate, write-tests-first pass (not a live-validation sign-off) | QA Engineer | Round 4 prose pass on new branch `content/prose-round-4` (off `main` @ `cd03314`, which already contains PR #11 and PR #12 — prior branches `content/project-portfolio-followup` and `content/tagline-tweak` are stale/merged, not reused). 5 planned edits to `public/index.html`, none yet implemented: (1) hero `.sub` paragraph — nobody had previously captured its exact text; located and confirmed verbatim as "7+ years making SaaS platforms more reliable, more scalable, and a little more AI-native. These days that's at Togetherwork, where I own reliability and observability across a big portfolio of products." → "7+ years making SaaS platforms more reliable, more scalable, and a little more cloud native. Right now, I own reliability and observability across a portfolio of products for a PE-backed SaaS."; (2) `.prose` first paragraph opener — "I'm at Togetherwork these days, standardizing how reliability..." → "These days, I'm standardizing how reliability..." (rest of paragraph, including "fragmented ecosystem"/"coaching engineering teams" wording, unchanged); (3) banking highlight bullet — "Owned an international core banking SaaS on Azure..." → "Appointed platform owner for an international core banking SaaS on Azure..."; (4) delivery & leadership skills cell — "SRE leadership, platform modernization, toil reduction, engineering enablement, operational coaching, observability strategy" → "Technical leadership, platform modernization, cross-functional collaboration, operational excellence, observability strategy"; (5) `h1.headline` tagline — "i build stuff that scales" → "I build stuff that scales" (capitalize leading "i" only, outside the `.token` span) | Red confirmed (as intended) — 5 failing / 57 passing across `e2e/homepage.spec.js`, `e2e/about.spec.js`, `e2e/projects.spec.js`, `e2e/contact.spec.js` | Read `public/index.html` cold (lines 34-165) to capture all 5 exact current strings before writing/updating any assertion, per the report-back-to-PM requirement for item (1). Checked all four spec files for existing coverage of the 5 target strings: `homepage.spec.js`'s `hero tagline drops the "and doesn't break" clause` test used `toContainText('i build stuff that scales')` — case-sensitive per Playwright's string-mode `toContainText`, so it would keep passing against a bare-lowercase-to-still-lowercase headline but gives no signal on capitalization; replaced it with `hero tagline capitalizes the leading "I"`, asserting `toContainText('I build stuff that scales')` plus an explicit `innerText().startsWith('I ')` check (old test's "and doesn't break" guard is retired as no-longer-at-risk — that wording shipped in PR #12 and can't silently reappear from a case-only edit). Added a new `hero sub-paragraph reflects the round-4 "PE-backed SaaS" reframe` test to `homepage.spec.js` (the `.sub` paragraph had zero dedicated coverage before this pass — only `about.spec.js`'s generic `shows 7+ years in the summary text` regex touches it, and that regex is unaffected since both old and new copy open with "7+ years"). In `about.spec.js`: added `bio paragraph opener drops the explicit "at Togetherwork" mention` (new-text-present / old-text-absent, alongside the still-valid existing `fragmented ecosystem`/`coaching engineering teams` test, which needed no change since that wording is untouched); updated the banking-bullet test to drop its now-obsolete `toContainText('Owned an international core banking SaaS')` positive assertion (kept only the still-relevant "if it broke"/arrow-wording regression guards from round 3) and added a new `banking platform highlight uses "appointed platform owner" wording` test asserting the new phrase present / old phrase absent; added `delivery & leadership cell reflects the round-4 rewording` (this cell had no prior coverage beyond the unaffected `observability strategy` phrase-migration test) asserting all 3 new phrases present, `platform modernization` still present, and all 4 dropped/reworded old phrases absent. Ran the full suite locally against **live staging** (`https://staging.ericreilly.com`, confirmed `200` via curl first) with `PLAYWRIGHT_BASE_URL=https://staging.ericreilly.com npx playwright test e2e/*.spec.js --reporter=list` (never via the CI live-E2E workflow, per CI-minute discipline): exactly 5 failed — one per planned edit, confirming 1:1 mapping and no test-authoring drift — and 57 passed, confirming zero regressions from the test-file edits. Verified suite hygiene: no `page.route()`/network interception anywhere under `e2e/` (RULE-1, confirmed via grep); no auth flow on this static site (RULE-4 N/A); all assertions are read-only GETs with no data creation (RULE-5 N/A, no cleanup needed); project continues its established `PLAYWRIGHT_BASE_URL`-driven single-config convention in place of a separate live config/`CI_LIVE_E2E` gate (accepted deviation, no mutation risk). Work done in an isolated git worktree (`git worktree add ../website-round4 -b content/prose-round-4 origin/main`) to avoid touching the pre-existing unrelated uncommitted WIP sitting in the primary working tree's `.project/*` files. `public/index.html` NOT modified — Frontend implements next. No PR opened, no tag pushed, no CI triggered. Next: Frontend Engineer implements all 5 copy changes together in `public/index.html`; QA re-runs this same suite to confirm green before sign-off. |
| 2026-09-12 | Local E2E, pre-merge (branch `content/prose-round-4`) | QA Engineer | **Independent QA validation pass** (distinct from this same date's TDD-gate red entry above) — re-run of the full suite against the *implemented* working-tree state on `content/prose-round-4`. Frontend Engineer's edits to `public/index.html` were uncommitted at the time of this run and self-reported as 62/62 passing locally. Per this session's established precedent (Code Reviewer blocked PR #11 for a self-reported-only pass; rounds 2 and 3 both required independent re-validation before PR/merge), QA independently re-ran the suite rather than accepting the self-report. Covers all 5 round-4 copy edits in one working tree: hero `.sub` paragraph reworded to drop "Togetherwork"/"AI-native" in favor of "cloud native"/"PE-backed SaaS" framing; bio-paragraph opener changed to "These days, I'm standardizing..."; banking highlight bullet changed to "Appointed platform owner for an international core banking SaaS..."; delivery & leadership skills cell reworded to "Technical leadership, platform modernization, cross-functional collaboration, operational excellence, observability strategy"; `h1.headline` tagline leading "i" capitalized to "I" | Pass — 62/62 (`e2e/homepage.spec.js`, `e2e/about.spec.js`, `e2e/projects.spec.js`, `e2e/contact.spec.js`) | Diffed `public/index.html` against `HEAD` (`ddfbf29`) first and confirmed all 5 edits matched the planned copy verbatim — no drift, no extra changes, no scope creep — before running any tests. Started a fresh `npx serve public -l 4173` (this story's established local-validation method); confirmed `/` returned `200` via curl before running the suite. Ran `PLAYWRIGHT_BASE_URL=http://localhost:4173 npx playwright test e2e/homepage.spec.js e2e/about.spec.js e2e/projects.spec.js e2e/contact.spec.js` (12 workers, default config): 62/62 passed in 4.6s, 0 failures, 0 flakes/retries — independently reproduces Frontend Engineer's reported 62/62, including all 5 assertions that were red in the TDD-gate entry above (tagline capitalization, hero sub-paragraph reframe, bio opener, banking bullet, delivery cell), now green. Server process stopped after the run (`taskkill` on the `npx serve` PID bound to port 4173). Verified suite hygiene: no `page.route()`/network interception anywhere under `e2e/` (RULE-1, confirmed via grep — none found); no auth flow on this static site (RULE-4 N/A); all 62 assertions are read-only GETs against rendered content with no data creation (RULE-5 N/A, no cleanup needed); project continues its established `PLAYWRIGHT_BASE_URL`-driven single-config convention in place of a separate live config/`CI_LIVE_E2E` gate variable (accepted deviation, no mutation risk in a read-only content suite). This satisfies the PR #11/#12 precedent: an independent QA re-run against the implemented working tree, corroborating (not merely repeating) the Frontend self-report. `public/index.html` committed alongside this sign-off entry per task instruction. QA sign-off GRANTED for this working-tree state; PR opened against `main` for Code Reviewer. Staging Definition-of-Done gate (live re-run post-merge/auto-deploy) still pending, per the PR #11/#12 pattern above — not yet requested. |
| 2026-09-12 | Live staging (`https://staging.ericreilly.com`) — Stage 5 Definition-of-Done gate | QA Engineer | **Round 3 of this gate on the same story class** — full live-environment validation of PR #13 (round 4: hero sub-paragraph, bio opener, banking bullet, delivery & leadership cell, tagline capitalization) after merge and auto-deploy. PR #13 merged to `main` as squash commit `b99703fa6eeea1e8f3ffc6480babdaa5074e0ee4`; the `Deploy Website` workflow (run `34718497465`, auto-triggered on push to `main`) completed with `deploy-staging`, `deploy-prompted-staging`, and `e2e-staging` all green. DevOps spot-checked `https://staging.ericreilly.com` via curl and confirmed all 5 content changes are live. This pass is the independent local re-run of that same gate ahead of human approval, per Definition of Done (staging E2E green + this report) | Pass — 62/62 (`e2e/homepage.spec.js`, `e2e/about.spec.js`, `e2e/projects.spec.js`, `e2e/contact.spec.js`) | Ran `PLAYWRIGHT_BASE_URL=https://staging.ericreilly.com npx playwright test e2e/homepage.spec.js e2e/about.spec.js e2e/projects.spec.js e2e/contact.spec.js --reporter=list` locally against the deployed staging URL (never via the CI live-E2E workflow — `e2e-staging` already ran once as part of the deploy and is a final safety net, not an iteration/validation tool). All three routes (`/`, `/projects`, `/contact`) confirmed `200` via curl before the run. Result: 62 passed, 0 failed, 5.3s, 0 flakes/retries — includes all 5 round-4-specific assertions plus the full pre-existing 57-test regression set, matching the pre-merge local-green count exactly and independently confirming DevOps's curl spot-check. Verified suite hygiene: no `page.route()`/network interception anywhere under `e2e/` (RULE-1 clean); no auth flow on this static site (RULE-4 N/A); all assertions are read-only GETs so no cleanup was required (RULE-5 N/A). Before touching git for this pass, `git status` in the shared working directory showed uncommitted changes to `.project/project-status.md`/`workflow-state.md` and an untracked `lessons-learned.md` — these were PM's own reconstruction of a real incident earlier this session (an un-isolated agent's branch checkout in this same shared directory discarded other uncommitted work). QA correctly stopped rather than stash/checkout past them, and reported back to PM instead of improvising; PM then committed those files directly (commit `d164c0b` on `content/prose-round-4`, cherry-picked to `main` as `0ead713`) before adding this sign-off entry, so no further data was put at risk. No tag pushed, no production deploy triggered, no CI live-E2E workflow re-run. QA sign-off GRANTED for staging; production promotion (tag push) is a separate, not-yet-requested step. |
| 2026-09-13 | N/A — TDD gate, write-tests-first pass (not a live-validation sign-off) | QA Engineer | Round 5: CTA copy tweak "get in touch" → "work with me" on new branch `content/cta-tweak` (off `main` @ `ef1d637`, which already contains PR #11/#12/#13 — prior branches `content/project-portfolio-followup`, `content/tagline-tweak`, `content/prose-round-4` are stale/merged, not reused). Verified `git status` clean on the shared working directory before branching (no repeat of the 2026-09-12 checkout incident). Searched all of `public/*.html` case-insensitively for "get in touch" and found 3 occurrences, none previously pinned by any test: (1) `public/index.html` line 45 — hero CTA `<span class="label">Get in touch</span>` linking to `/contact`, the clearest nav/CTA match the task described; (2) `public/contact.html` line 7 — `<meta name="description">` reading "Get in touch with Eric Reilly: SRE and cloud architecture consulting via White Glove Solutions."; (3) `public/projects.html` line 185 — prose inside the Personal Website card's "Why I built this" section, "...give people a way to get in touch." Also found 3 more occurrences outside `public/*.html` (`src/pages/Contact.js` "Get In Touch" heading, `src/pages/Projects.js` same prose sentence, `recovered-build/static/js/main.*.js[.map]` bundled output) — confirmed out of scope per `.project/project-status.md` ("the `src/` React app is legacy and not part of the build"; `recovered-build/` is a similarly-inert artifact, not referenced by `npm run build`, which only copies `public/*.html` verbatim) | Red confirmed (as intended) — 3 failing / 70 passing across `e2e/homepage.spec.js`, `e2e/contact.spec.js`, `e2e/projects.spec.js` (plus `e2e/about.spec.js`/`e2e/navigation.spec.js` unaffected and green) | Added 3 new-text-present/old-text-absent tests, one per occurrence: `hero contact CTA reads "Work with me", not "Get in touch"` in `homepage.spec.js` (targets `.hero-links a[href="/contact"] .label`, straightforward 1:1 label swap); `meta description reflects "work with me" CTA framing, not "get in touch"` in `contact.spec.js`; `Personal Website "why I built this" reflects "work with me" phrasing, not "get in touch"` in `projects.spec.js` (targets the `.why` block inside the `rowFor(page, 'Personal Website')` row helper already used elsewhere in that file). Flagging one copy decision (Tier 2, autonomy-bar): a literal substring swap breaks the meta-description sentence ("Get in touch with Eric Reilly: ..." → "Work with me with Eric Reilly: ...", ungrammatical), so the test instead pins "Work with me: SRE and cloud architecture consulting via White Glove Solutions." (drops the redundant "with Eric Reilly"). Frontend/PM should confirm this exact wording — or update the test — before implementing; the other two occurrences (`index.html` label, `projects.html` prose) are clean 1:1 swaps with no such issue. Ran the full suite against a from-scratch local static server per this project's established pre-implementation convention (`npx serve public -l 4173`, confirmed `/`, `/projects`, `/contact` all `200` via curl first): `PLAYWRIGHT_BASE_URL=http://localhost:4173 npx playwright test e2e/homepage.spec.js e2e/contact.spec.js e2e/projects.spec.js e2e/about.spec.js e2e/navigation.spec.js --reporter=list` — exactly 3 failed (the 3 new assertions, 1:1 with the 3 in-scope occurrences) / 70 passed, confirming zero regressions from the test-file edits. Verified suite hygiene: no `page.route()`/network interception anywhere under `e2e/` (RULE-1, confirmed via grep, no matches); no auth flow on this static site (RULE-4 N/A); all assertions are read-only GETs with no data creation (RULE-5 N/A, no cleanup needed); project continues its established `PLAYWRIGHT_BASE_URL`-driven single-config convention (accepted deviation, no mutation risk). Server stopped after the run. `public/*.html` NOT modified — Frontend implements next. No PR opened, no tag pushed, no CI triggered. Next: Frontend Engineer implements the copy change across all 3 occurrences (confirming the meta-description wording above first); QA re-runs this same suite to confirm green before sign-off. |
| 2026-09-13 | Local E2E, pre-merge (branch `content/cta-tweak`) | QA Engineer | **Independent QA validation pass** (distinct from this same round's TDD-gate red entry above) — re-run of the full suite against the *implemented* working-tree state on `content/cta-tweak`. Frontend Engineer implemented all 3 planned edits directly in `public/index.html`, `public/contact.html`, and `public/projects.html` (uncommitted at the time of this run) and self-reported 73/73 passing, having run the full `e2e/` directory (all 5 spec files, including `navigation.spec.js`, not just the 3 originally-named specs — accounting for the 73-vs-70-vs-3-files count discrepancy: 70 pre-existing passing tests from the TDD-gate entry + 3 newly-green assertions = 73, and `navigation.spec.js`'s 8 tests were always part of the full suite, simply not re-stated by name in that earlier entry). Per this session's established precedent (Code Reviewer blocked PR #11 for a self-reported-only pass; every subsequent round required independent re-validation before PR/merge), QA independently re-ran the suite rather than accepting the self-report | Pass — 73/73 across all 5 spec files (`about.spec.js`, `contact.spec.js`, `homepage.spec.js`, `navigation.spec.js`, `projects.spec.js`) | Ran `git status` first per task instruction and confirmed only the 3 expected files were modified (no unexpected drift in the shared working directory, no repeat of the 2026-09-12 checkout incident). Diffed all 3 files against `HEAD` and confirmed each edit matched the planned copy verbatim and exactly as pinned by the round-5 TDD-gate tests: `index.html` hero CTA label "Get in touch" → "Work with me"; `contact.html` meta description → "Work with me: SRE and cloud architecture consulting via White Glove Solutions." (matches the exact wording QA flagged and pinned in the TDD-gate entry, confirming Frontend used QA's suggested grammar fix rather than a literal broken substring swap); `projects.html` Personal Website "why I built this" prose → "...give people a way to work with me." No drift, no extra changes, no scope creep beyond the 3 files. Started a fresh `npx serve public -l 5050` (this story's established local-validation method — not `python -m http.server` on `build/`, which does not replicate clean-URL routing); confirmed `/` returned `200` via curl (redirect-following, since `serve` 301s bare requests without `-L`) before running the suite. Ran `PLAYWRIGHT_BASE_URL=http://localhost:5050 npx playwright test` with no path filter (full `e2e/` directory, default config, 12 workers): 73 passed in 4.6s, 0 failures, 0 flakes/retries — independently reproduces and confirms Frontend Engineer's reported 73/73, including all 3 assertions that were red in the TDD-gate entry above, now green, plus zero regressions across the other 70. Server process stopped after the run. Verified suite hygiene: no `page.route()`/network interception anywhere under `e2e/` (RULE-1, confirmed via grep, no matches); no auth flow on this static site (RULE-4 N/A); all 73 assertions are read-only GETs against rendered content with no data creation (RULE-5 N/A, no cleanup needed); project continues its established `PLAYWRIGHT_BASE_URL`-driven single-config convention (accepted deviation, no mutation risk in a read-only content suite). This satisfies the established precedent: an independent QA re-run against the implemented working tree, corroborating (not merely repeating) the Frontend self-report, and resolving the 73-vs-3-files count question raised in the task. `public/index.html`, `public/contact.html`, `public/projects.html` committed alongside this sign-off entry. QA sign-off GRANTED for this working-tree state; PR opened against `main` for Code Reviewer. Staging Definition-of-Done gate (live re-run post-merge/auto-deploy) still pending, per the established pattern above — not yet requested. |
| 2026-09-13 | Live staging (`https://staging.ericreilly.com`) — Stage 5 Definition-of-Done gate | QA Engineer | **Round 4 of this gate on the same story class** — full live-environment validation of PR #14 (round 5: CTA copy tweak, "get in touch" → "work with me" across `index.html`/`contact.html`/`projects.html`) after merge and auto-deploy. PR #14 merged to `main` as squash commit `f2266c73b4bfcef3fb41ec0ce6a47601dd3ca31f`; the `Deploy Website` workflow (run `34763041660`) completed with all jobs green. DevOps spot-checked staging via curl and confirmed all 3 content changes are live. This is the last content round before a production tag push, making this sign-off the final gate before PM proposes promoting to prod | Pass — 73/73 across all 5 spec files (`about.spec.js`, `contact.spec.js`, `homepage.spec.js`, `navigation.spec.js`, `projects.spec.js`) | Ran `git status` in the shared working directory first per task instruction — clean, on `content/cta-tweak`, up to date with origin, no unexpected uncommitted `.project/*.md` changes (no repeat of the 2026-09-12 checkout incident). Fetched `origin/main` and confirmed it sits exactly at the expected squash commit `f2266c73b4bfcef3fb41ec0ce6a47601dd3ca31f` with zero diff against `content/cta-tweak`'s tip, then checked out clean `main` to run from (`git checkout main && git pull`). Confirmed `https://staging.ericreilly.com/`, `/projects`, `/contact`, and `/about` all returned `200` via curl before running the suite. Verified suite hygiene: no `page.route()`/network interception anywhere under `e2e/` (RULE-1, confirmed via grep, no matches); no auth flow on this static site (RULE-4 N/A); all assertions are read-only GETs with no data creation (RULE-5 N/A, no cleanup needed); project continues its established `PLAYWRIGHT_BASE_URL`-driven single-config convention in place of a separate live config/`CI_LIVE_E2E` gate variable (accepted deviation, mirrored by CI's own `e2e-staging` job, no mutation risk in a read-only content suite). Ran `PLAYWRIGHT_BASE_URL=https://staging.ericreilly.com npx playwright test --reporter=list` locally (never via the CI live-E2E workflow — `e2e-staging` already ran once as part of the deploy and is a final safety net, not an iteration/validation tool per CI-minute discipline): 73 passed in 9.6s, 0 failures, 0 flakes/retries, 0 retries configured — includes all 3 CTA-copy assertions plus the full pre-existing 70-test regression set, matching the pre-merge local-green count exactly and independently confirming DevOps's curl spot-check. No files modified other than this entry; `public/*.html` and `e2e/*.spec.js` untouched; no test files edited. No tag pushed, no production deploy triggered, no CI live-E2E workflow re-run. QA sign-off GRANTED for staging. This closes out the last content round in the prose-refresh/CTA story — production promotion (tag push) is a separate, PM-owned decision now that all rounds are staging-green. |
| 2026-09-13 | N/A — TDD gate, write-tests-first pass (not a live-validation sign-off) | QA Engineer | Round 6: hero `.sub` paragraph final-clause reorder on new branch `content/portfolio-wording` (off `main` @ `fe0ce66`, current prod is live as `v0.1.6`). Verified `git status` clean on the shared working directory before branching (no repeat of the 2026-09-12 checkout incident). Planned edit, not yet implemented: "...Right now, I own reliability and observability across a portfolio of products for a PE-backed SaaS." → "...across a portfolio of PE-backed SaaS products." (clause reorder plus a trailing period retained for correct sentence-final grammar — the sentence already ended with a period in both versions, so no punctuation gap is introduced). Confirmed the described current text is exactly what's live in production: `curl`'d `https://ericreilly.com/` directly (this content is live, so no local/staging check was needed for the baseline) and extracted the literal string `across a portfolio of products for a PE-backed SaaS.` — matches verbatim | Red confirmed (as intended) — 1 failing / 73 passing across the full `e2e/` suite (74 total) | Checked `e2e/homepage.spec.js`'s existing round-4 test (`hero sub-paragraph reflects the round-4 "PE-backed SaaS" reframe`, added 2026-09-12) for pinning risk: it asserts `toContainText('more cloud native')`, `toContainText('PE-backed SaaS')`, `not.toContainText('AI-native')`, `not.toContainText("that's at")` — none of these distinguish old vs. new clause order (`PE-backed SaaS` is a substring of both `products for a PE-backed SaaS` and `PE-backed SaaS products`), so that test stays green unchanged and does **not** serve as a gate for this edit; confirmed this by running it standalone against production first (passed, as expected, before any test-file edit). Left it in place as-is with an updated comment noting it's now a stable regression guard for the round-4 wording rather than an active TDD gate. Added a new dedicated test, `hero sub-paragraph reflects the round-6 "PE-backed SaaS products" reorder`, asserting `toContainText('a portfolio of PE-backed SaaS products.')` (pins the exact new ending, including the trailing period) and `not.toContainText('portfolio of products for a PE-backed SaaS')` (old ordering absent). No other spec file (`about.spec.js`, `contact.spec.js`, `projects.spec.js`, `navigation.spec.js`) references this phrase — grepped `portfolio`/`PE-backed`/`SaaS` across all of `e2e/` and confirmed the only other `portfolio` hits are `about.spec.js`'s unrelated "AWS DevOps Agent across the portfolio" bullet, untouched by this edit. Ran the new test in isolation first (`npx playwright test e2e/homepage.spec.js -g "PE-backed SaaS"`) against live `https://ericreilly.com` (default `playwright.config.js` baseURL — this content is live in prod, matching this project's established practice of validating pre-branch TDD gates directly against whichever environment currently serves the content under test): round-4 test passed, round-6 test failed exactly as expected, with the actual/expected diff in the failure output confirming the assertion is wired to the correct old-vs-new text. Then ran the full suite with no path filter (`npx playwright test --reporter=list`, default config, all 5 spec files): 73 passed / 1 failed (the new round-6 test) in 14.9s, 0 flakes/retries, confirming zero regressions from the test-file edit and exactly 1 red test tied 1:1 to the single planned edit. Verified suite hygiene: no `page.route()`/network interception anywhere under `e2e/` (RULE-1, confirmed via grep, no matches); no auth flow on this static site (RULE-4 N/A); all assertions are read-only GETs with no data creation (RULE-5 N/A, no cleanup needed); project continues its established default-`playwright.config.js`-baseURL convention for pre-branch checks against whichever environment currently holds the content (accepted deviation, no mutation risk). `public/index.html` NOT modified — Frontend implements next; `e2e/homepage.spec.js` changes not yet committed at time of this entry (committed alongside it). No PR opened, no tag pushed, no CI triggered. Next: Frontend Engineer changes the single clause in `public/index.html`; QA re-runs `e2e/homepage.spec.js` (or the full suite) to confirm green before sign-off. Human has indicated intent to ship straight to production once validated — staging Definition-of-Done gate still applies per this story's established route-to-live before any prod tag. |
| 2026-09-13 | N/A — TDD gate, write-tests-first pass (not a live-validation sign-off) | QA Engineer | **Round 6, expanded scope** (PM bundled a second edit into `content/portfolio-wording` at the human's request, to ship both together): the `.prose` first-paragraph opener in `public/index.html`. Confirmed current live text via `curl` against `https://ericreilly.com/` directly, same method as the hero-sub-paragraph check above: extracted the literal string "These days, I'm standardizing how reliability and observability work across a pretty big SaaS portfolio. Some weeks that means cutting incident noise. Other weeks it's modernizing an older platform, wrangling a fragmented ecosystem into something consistent, or coaching engineering teams in the ways of SRE best practices." — matches verbatim. Planned edit, not yet implemented: only the opening clause changes to "These days, I'm standardizing reliability across a big SaaS portfolio." (drops "how... and observability work" and "pretty", adds the article "a" before "big SaaS portfolio" — PM's deliberate grammar call, flagged to the human). Everything from "Some weeks that means cutting incident noise." onward is UNCHANGED | Red confirmed (as intended) — 2 failing / 72 passing across the full `e2e/` suite (74 total): the new bio-opener assertion plus the still-outstanding hero-sub-paragraph assertion from the entry above (not yet implemented either — both ship together in this round) | Checked `e2e/about.spec.js` for existing coverage: (1) `bio paragraph reflects new ecosystem/coaching language` (line 32) asserts `fragmented ecosystem` / `coaching engineering teams` present and `fragmented tenant landscape` absent — all in the UNCHANGED latter portion of the paragraph, confirmed still valid by running it standalone against production first (passed, unaffected, as expected — no edit needed); (2) `bio paragraph opener drops the explicit "at Togetherwork" mention` (round-4 test, line 39) asserted `toContainText("These days, I'm standardizing how reliability")` — this positive assertion is now INVALIDATED by the round-6 edit (the new opener drops the word "how" immediately before "reliability", so the exact substring will no longer match once implemented), unlike the hero-sub-paragraph case where the old assertion happened to remain a true substring. Updated this test in place (renamed to `bio paragraph opener reflects the round-6 "standardizing reliability" wording`, following the same in-place-supersession pattern used for the round-4 tagline-capitalization test in `homepage.spec.js`, since the old assertion is no longer a valid regression guard): new positive assertion `toContainText("These days, I'm standardizing reliability across a big SaaS portfolio.")`, plus three `not.toContainText` guards — `"I'm at Togetherwork these days"` (still-valid round-4 regression guard, carried forward unchanged), `'standardizing how reliability and observability work'`, and `'pretty big SaaS portfolio'` (both new, pinning the round-6 wording specifically). Comment block documents the full round-4 → round-6 history on this same clause so a future reader isn't left guessing why the assertion changed twice. No other spec file references this phrase (grepped `standardizing`/`fragmented ecosystem`/`coaching engineering`/`portfolio` across all of `e2e/`; the only other `portfolio` hits are the already-noted unrelated "AWS DevOps Agent across the portfolio" bullet and this round's own hero-sub-paragraph edit). Ran the updated test in isolation first (`npx playwright test e2e/about.spec.js -g "bio paragraph"`) against live `https://ericreilly.com`: the ecosystem/coaching test passed (confirming no regression), the round-6 opener test failed exactly as expected, with the actual/expected diff in the failure output showing the full old paragraph text against the new expected substring. Then ran the full suite with no path filter: 72 passed / 2 failed (this new/updated test plus the still-outstanding round-6 hero-sub-paragraph test from the prior entry, since that edit also hasn't landed yet) in 15.0s, 0 flakes/retries — confirms exactly 2 red tests tied 1:1 to the 2 planned edits in this round, zero unintended regressions. Verified suite hygiene: no `page.route()`/network interception anywhere under `e2e/` (RULE-1, confirmed via grep, no matches); no auth flow on this static site (RULE-4 N/A); all assertions are read-only GETs with no data creation (RULE-5 N/A, no cleanup needed). `public/index.html` NOT modified — Frontend implements both edits together next; `e2e/about.spec.js` changes committed to `content/portfolio-wording` alongside `e2e/homepage.spec.js`'s prior commit. No PR opened, no tag pushed, no CI triggered. Both edits (hero sub-paragraph ending + bio opener) are now test-ready and confirmed red together on the same branch — reported back to PM for Frontend handoff. |
| 2026-09-13 | Local E2E, pre-merge (branch `content/portfolio-wording`) | QA Engineer | **Independent QA validation pass** (distinct from this same round's two TDD-gate red entries above) — re-run of the full suite against the *implemented* working-tree state on `content/portfolio-wording`. Frontend Engineer implemented both round-6 edits directly in `public/index.html` (uncommitted at the time of this run) and self-reported 74/74 passing locally. Per this session's established precedent (every round since PR #11 has required independent re-validation before commit/PR, never accepting a same-session self-report), QA independently re-ran the suite rather than accepting the self-report. This content is live in production; the human has indicated intent to ship ASAP once validated | Pass — 74/74 across all 5 spec files (`about.spec.js`, `contact.spec.js`, `homepage.spec.js`, `navigation.spec.js`, `projects.spec.js`) | Ran `git status` first per task instruction and confirmed only `public/index.html` was modified — no unexpected drift in the shared working directory. Diffed `public/index.html` against `HEAD` and confirmed both edits matched the planned copy verbatim and exactly as pinned by the two round-6 TDD-gate tests above: hero `.sub` paragraph ending changed to "...across a portfolio of PE-backed SaaS products." (clause reorder, trailing period retained); `.prose` first-paragraph opener changed to "These days, I'm standardizing reliability across a big SaaS portfolio." with everything from "Some weeks that means cutting incident noise." onward byte-for-byte unchanged. No drift, no extra changes, no scope creep. Started a fresh `npx serve public -l 5050` (this story's established local-validation method); confirmed `/` returned `200` via curl before running the suite. Ran `PLAYWRIGHT_BASE_URL=http://localhost:5050 npx playwright test` with no path filter (full `e2e/` directory, default config, 12 workers): 74 passed in 7.3s, 0 failures, 0 flakes/retries — independently reproduces Frontend Engineer's reported 74/74, including both assertions that were red in the TDD-gate entries above (`hero sub-paragraph reflects the round-6 "PE-backed SaaS products" reorder`, `bio paragraph opener reflects the round-6 "standardizing reliability" wording`), now green, plus zero regressions across the other 72. Server process stopped after the run. Verified suite hygiene: no `page.route()`/network interception anywhere under `e2e/` (RULE-1, confirmed via grep, no matches); no auth flow on this static site (RULE-4 N/A); all 74 assertions are read-only GETs against rendered content with no data creation (RULE-5 N/A, no cleanup needed); project continues its established `PLAYWRIGHT_BASE_URL`-driven single-config convention (accepted deviation, no mutation risk in a read-only content suite). This satisfies the established precedent: an independent QA re-run against the implemented working tree, corroborating (not merely repeating) the Frontend self-report. `public/index.html` committed alongside this sign-off entry. QA sign-off GRANTED for this working-tree state; PR opened against `main`. Staging Definition-of-Done gate (live re-run post-merge/auto-deploy) still applies per this story's established route-to-live before any prod tag, notwithstanding the human's stated urgency to ship — no tag pushed, no deploy triggered, PR not merged by QA. |
| 2026-09-13 | Live staging (`https://staging.ericreilly.com`) — Stage 5 Definition-of-Done gate | QA Engineer | **Round 5 of this gate on the same story class** — full live-environment validation of PR #15 (round 6: hero sub-paragraph ending reordered to "...a portfolio of PE-backed SaaS products.", bio-paragraph opener trimmed to "These days, I'm standardizing reliability across a big SaaS portfolio.") after merge and auto-deploy. PR #15 merged to `main` as squash commit `270558e83b6062ada141a19a3bb5633cd9d7efc9`; the `Deploy Website` workflow (run `34795666261`, auto-triggered on push to `main`) completed successfully. DevOps spot-checked `https://staging.ericreilly.com` via curl and confirmed both content changes are live. The human wants this shipped to production immediately once this validation is green | Pass — 74/74 across all 5 spec files (`about.spec.js`, `contact.spec.js`, `homepage.spec.js`, `navigation.spec.js`, `projects.spec.js`) | Ran `git status` in the shared working directory first per task instruction — clean, no unexpected uncommitted `.project/*.md` changes (no repeat of the 2026-09-12 checkout incident). Fetched `origin/main` and confirmed it sits exactly at the expected squash commit `270558e83b6062ada141a19a3bb5633cd9d7efc9` (`fe0ce66..270558e`), then checked out clean `main` and fast-forwarded (`git checkout main && git pull --ff-only`). Confirmed `https://staging.ericreilly.com/`, `/projects`, `/contact`, and `/about` all returned `200` via curl, and independently re-confirmed both round-6 content strings live via curl grep (`a portfolio of PE-backed SaaS products.` and `These days, I'm standardizing reliability across a big SaaS portfolio.`) before running the suite — corroborating, not just trusting, DevOps's spot-check. Verified suite hygiene: no `page.route()`/network interception anywhere under `e2e/` (RULE-1, confirmed via grep, no matches); no auth flow on this static site (RULE-4 N/A); all assertions are read-only GETs with no data creation (RULE-5 N/A, no cleanup needed); project continues its established `PLAYWRIGHT_BASE_URL`-driven single-config convention in place of a separate live config/`CI_LIVE_E2E` gate variable (accepted deviation, mirrored by CI's own `e2e-staging` job, no mutation risk in a read-only content suite). Ran `PLAYWRIGHT_BASE_URL=https://staging.ericreilly.com npx playwright test --reporter=list` locally, full `e2e/` directory, no path filter (never via the CI live-E2E workflow — CI's own `e2e-staging` job already ran once as part of the deploy and is a final safety net, not an iteration/validation tool per CI-minute discipline): 74 passed in 10.7s, 0 failures, 0 flakes/retries, 0 retries configured — includes both round-6 assertions (`hero sub-paragraph reflects the round-6 "PE-backed SaaS products" reorder`, `bio paragraph opener reflects the round-6 "standardizing reliability" wording`) plus the full pre-existing 72-test regression set, matching the pre-merge local-green count exactly. No files modified other than this entry; `public/*.html` and `e2e/*.spec.js` untouched; no test files edited. No tag pushed, no production deploy triggered, no CI live-E2E workflow re-run. QA sign-off GRANTED for staging. This is the last content round before a production tag push per the human's stated intent — PM/human may proceed to promote to production. |
