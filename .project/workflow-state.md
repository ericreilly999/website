---
# Workflow State

## Route-to-Live
**Standard** — `main → staging (auto) → prod (tag)`

Push to `main` auto-deploys to `staging.ericreilly.com` (and `staging.prompted.ericreilly.com`) and runs the Playwright E2E gate against staging. A semver tag push (`vX.Y.Z`) deploys to production (`ericreilly.com` and `prompted.ericreilly.com`).

## Current Stage
**Live / Ongoing Maintenance — CLD-13 (light/dark theme toggle) shipped to production as `v0.1.8`. Story closed.**

## CLD-13 — Production Promotion (2026-09-21)

**Shipped end-to-end: spec → tests → dev → staging DoD → human approval → prod tag → prod deploy → prod QA confirmation.**

- **Authorization:** Fleet Decisions page, decision `WS-2`, `answer.decided_by_owner === true`, decided `2026-09-21T16:57:51.703Z`. Full detail in `.project/decisions.md` (2026-09-21 entry).
- **Tag:** `v0.1.8` (annotated), cut from `main` @ `77951eb` (docs-only since staging sign-off `b23d4a9` — zero app-code drift). Semver reconciliation (chose `v0.1.8` over the orphaned `v1.0.0`–`v1.1.3` series from an April 2026 CI-debugging episode) recorded in `.project/decisions.md`.
- **Deploy:** Workflow run [35675110707](https://github.com/ericreilly999/website/actions/runs/35675110707) — `Deploy Production` ✅, `Deploy Prompted Production` ✅. Logged in `.project/deployment-log.md` (DevOps, commit `5f06f30`).
- **Prod QA validation:** 95/95 passing, run locally against `https://ericreilly.com` per ci-discipline (never via live-E2E CI). Toggle confirmed present/functional/persistent on all 3 main-site pages (`/`, `/projects`, `/contact`); dark remains default. `.project/test-signoff.md` commit `b38827e`.
- **Scope clarification:** `prompted.ericreilly.com` does not carry the toggle — it's a fully self-contained static page with no `shared.css`/theme JS, matching `spec/light-mode-toggle.md`'s scope (main site only, 3 pages). Not a gap; recorded for traceability since it wasn't obvious without checking.
- **Definition of Done: MET.** All three elements — QA sign-off, docs in the same merge (PR #17), human approval of the staging gate (then prod promotion) — are satisfied.

## Prior Rounds — Content Refresh (closed, `v0.1.7`)

Prior content-refresh story shipped to prod as `v0.1.5`. Five further rounds of copy edits were made 2026-09-12/13, all merged, staged, QA-signed-off, and now live in production:
- **Round 2** (PR #11, `9c76ac1`) — bio lede/paragraph, highlights list, skills grid.
- **Round 3** (PR #12, `6538352`) — hero tagline shortened, 3 highlight bullets reworded.
- **Round 4** (PR #13, `b99703f`) — hero sub-paragraph reworded (drops "Togetherwork"/"AI-native"), bio opener reworded, banking bullet reworded ("Appointed platform owner for..."), delivery & leadership skills cell reworded, tagline "i"→"I" capitalized.
- **Round 5** (PR #14, `f2266c7`) — CTA text "Get in touch" → "Work with me" (hero button, contact meta description, projects prose).
- **Round 6** (PR #15, `270558e`) — hero sub-paragraph ending reordered ("a portfolio of PE-backed SaaS products."), bio opener trimmed ("standardizing reliability across a big SaaS portfolio").

Human approved production promotion twice: 2026-09-13 for `v0.1.6` ("I think this is good to ship to production"), then again for `v0.1.7` ("ship to prod immediately"). Tag `v0.1.7` pushed from `main` @ `861910f`, `deploy-prod` + `deploy-prompted-prod` both green, live content independently verified via curl against `https://ericreilly.com`.

## Last Gate Cleared (prior prod story)
Prod Deploy — human explicitly approved ("ship the changes to prod"). Tagged and pushed `v0.1.5` from `main` tip `afac3632`.
- Workflow run [34302193693](https://github.com/ericreilly999/website/actions/runs/34302193693): `deploy-prod` ✅ `deploy-prompted-prod` ✅.
- Live production verified twice. No discrepancies.

## Active Route

**Round 2 (PR #11) — COMPLETE through Stage 5.** QA wrote/updated assertions → Frontend implemented → Docs (no impact) → Code Reviewer blocked once (missing independent QA re-validation) → QA independent pass (31/31) → Code Reviewer merged (`9c76ac1`) → staging deploy green (run 34702470524) → QA staging sign-off (31/31, `test-signoff.md` commit `92f2458`).

**Round 3 (PR #12) — COMPLETE through Stage 5.** QA wrote/updated assertions (`homepage.spec.js`, `about.spec.js`) → Frontend implemented → QA independent pass (58/58, built in proactively) → Docs (no impact) → Code Reviewer merged clean (`6538352`) → staging deploy green (run [34704340442](https://github.com/ericreilly999/website/actions/runs/34704340442)) → QA staging sign-off (58/58, `test-signoff.md` commit `cd03314`).

**Round 4 (PR #13) — COMPLETE through Stage 5.** QA located strings, wrote/updated assertions (5 checks, commit `ddfbf29`, confirmed 5 red/57 green) → Frontend implemented → QA independent pass (62/62, commit `9a4febf`) → PR #13 opened → Documentation Agent (no doc impact) → Code Reviewer merged clean (`b99703f`) → staging deploy green (run [34718497465](https://github.com/ericreilly999/website/actions/runs/34718497465)) → QA staging sign-off (62/62).

**Round 5 (PR #14) — COMPLETE through Stage 5.** QA located 3 "get in touch" occurrences, wrote assertions (commit `ccdb092`, confirmed 3 red/70 green) → Frontend implemented → QA independent pass (73/73, commit `cb283eb`) → PR #14 opened → Documentation Agent (no doc impact) → Code Reviewer merged clean (`f2266c7`) → staging deploy green (run [34763041660](https://github.com/ericreilly999/website/actions/runs/34763041660)) → QA staging sign-off (73/73, `test-signoff.md` commit `fc100b3`).

**Round 6 (PR #15) — COMPLETE through Stage 5.** QA located both strings, wrote assertions (commits `d487e1b`, `6a02850`, confirmed 2 red/72 green) → Frontend implemented → QA independent pass (74/74, commit `fb47468`) → PR #15 opened → Documentation Agent (no doc impact) → Code Reviewer merged clean (`270558e`) → staging deploy green (run [34795666261](https://github.com/ericreilly999/website/actions/runs/34795666261)) → QA staging sign-off (74/74, `test-signoff.md` commit `861910f`).

## Linear Onboarding — 2026-09-19
Onboarded to the fleet Linear dashboard per `~/.claude/docs/agent-conventions/linear.md`. Project `website` created under team `Claude's Projects` (CLD): https://linear.app/drinkupapp/project/website-b1cbb4812a78. `Linear project:` line recorded in `CLAUDE.md`. Three epics created (status per `list_issue_statuses`, no duplicate-name ambiguity hit for `Done`/`Planning`):
- `CLD-12` — `[100%] Content refresh — prose rounds 2-6` — **Done**
- `CLD-13` — `[0%] Light/dark theme toggle` — **Planning**
- `CLD-14` — `[0%] Ops & maintenance backlog` — **Planning**

Same session: recovered uncommitted PM/RA artifacts from an interrupted 2026-09-14 session (`.project/TODO.md` QA-07..09/DEV-05..06, `.project/decisions.md` Tier 2 record, new `spec/light-mode-toggle.md`) — verified genuine, committed to `main` (`08b2886`). This is the source scope behind `CLD-13`; not started this session (no QA test-writing dispatched) since this run's focus was Linear standardization, not new dev. Ready to pull next session via normal Stage 1 (QA Engineer).

## Active Route — CLD-13 Light/Dark Theme Toggle (2026-09-20)

Human direction this session (verbatim, recorded in `.project/decisions.md`): **"build the light/dark mode toggle and ship to staging."** Pulled `CLD-13` into the active sprint. Session-open ritual clean: `main` @ `a62d012` (PR #16, SES DKIM for gencast, merged same day, unrelated to this feature), no open PRs, no `deploy-failure` issues, no orphaned in-flight work.

Plan: Stage 1 (QA writes `e2e/theme-toggle.spec.js` for QA-07..QA-09 against `spec/light-mode-toggle.md`) → Stage 2 (Frontend implements DEV-05/DEV-06) → Stage 3 (Documentation Agent → Code Reviewer → merge) → Stage 4 (auto staging deploy, PM blocks on foreground until-loop) → Stage 5 (QA full E2E sign-off on `staging.ericreilly.com`). **Stops at staging** — prod tag push is Tier 3 (human explicitly deferred approval to after viewing staging); queued, not executed, this session.

Fleet-mode note: dispatched engineers use `isolation: "worktree"`, git-writing dispatches serialized (QA → Frontend → Docs → Code Reviewer, one at a time) per this run's override instructions.

**Stage 1 — COMPLETE.** QA Engineer wrote `e2e/theme-toggle.spec.js` (21 tests: QA-07 toggle presence/accessibility, QA-08 behavior/persistence/FOUC/storage-failure, QA-09 light-mode computed-style + hardcoded-hex regression) on branch `feature/light-dark-toggle` off `main` @ `042c982` (commits `7b7335b` spec file, `171d539` TODO.md). Confirmed RED for the right reason (19 failed/2 passed against prod, all "element not found"/`data-theme` unset — not broken selectors), and zero regression on the 5 pre-existing spec files (74/74 still green). `.project/TODO.md` QA-07/QA-08/QA-09 now `[x]`. Branch pushed to `origin/feature/light-dark-toggle`.

**Stage 2 — COMPLETE.** Frontend Engineer implemented DEV-05 (light theme CSS override block + new `--on-accent`/`--error`/`--error-text`/`--select-chevron` variables + hardcoded-hex fixes in `shared.css`) and DEV-06 (toggle button + synchronous head theme-init script + click handler on all 3 pages), commit `1f9b17d` on `feature/light-dark-toggle`. 95/95 tests passing locally (21 new + 74 pre-existing). Contrast independently re-verified against spec §5.4 (tightest margin 4.48:1, clears 3:1 UI-component minimum). PR **#17** opened against `main`: https://github.com/ericreilly999/website/pull/17. `.project/TODO.md` DEV-05/DEV-06 now `[x]`.

Documentation Agent updated `README.md` (dark-theme-only language was stale) on `feature/light-dark-toggle`, commit `be3fbd8`. No `docs/` directory exists in this repo; `docs/api/` not implicated. Flagged `CLAUDE.md`'s similar stale "shared dark-theme design system" line as a follow-up (out of Documentation Agent's `README.md`/`docs/` boundary — PM to action).

Code Reviewer reviewed PR #17 — **REQUEST CHANGES, not merged.** Implementation itself checked out clean (FOUC script synchronous in `<head>` on all 3 pages, `localStorage` failure fallback intact, all hardcoded-hex call sites now `var(...)`-driven, Tier 2 decisions matched). **Blocking finding:** no independent QA validation exists for this PR — only Frontend Engineer's self-reported "95/95 passing," no `test-signoff.md` entry, no QA PR comment. Every prior round (PR #11-#15) went through an independent QA re-validation pass between dev and merge; this round skipped it. Comment: https://github.com/ericreilly999/website/pull/17#issuecomment-5754269056

QA Engineer independent validation: **95/95 passing** (21 new theme-toggle tests + 74 pre-existing), confirmed independently on `feature/light-dark-toggle` @ `be3fbd8`. `test-signoff.md` entry committed `982a3b5` (direct-to-branch, matching this file's established precedent). PR comment: https://github.com/ericreilly999/website/pull/17#issuecomment-5754289600

**Stage 3 — COMPLETE.** Code Reviewer re-reviewed with independent QA corroboration in hand, verified the sign-off entry and PR comment directly, confirmed no implementation drift since first review, and merged PR #17 (squash `b23d4a9`, `feature/light-dark-toggle` retired). `main` now at `b23d4a9`.

**Stage 4 — COMPLETE.** Note: `deploy.yml` has no `paths:` filter (pre-existing known risk, see below), so each PM docs-only commit during Stage 3 re-triggered/cancelled the in-flight staging run until the final commit's run completed — the completed run (35551660561, "docs: log CLD-13 Stage 3 complete...") is downstream of `b23d4a9` and deploys the feature correctly. Workflow run [35551660561](https://github.com/ericreilly999/website/actions/runs/35551660561): `Deploy Staging` ✅, `Deploy Prompted Staging` ✅, `E2E Tests (Staging)` ✅ (CI's own smoke gate). `Deploy Production`/`Deploy Prompted Production` correctly skipped (no tag pushed). Staging live at `staging.ericreilly.com` / `staging.prompted.ericreilly.com`.

**Stage 5 — COMPLETE. QA sign-off GRANTED on staging.** 95/95 passing against live `https://staging.ericreilly.com` (21 new theme-toggle tests + 74 pre-existing), run locally per ci-discipline (never via the live-E2E CI workflow). `test-signoff.md` entry committed `68f10c2`. Non-blocking finding: pre-merge sign-off's "clean RULE-1" claim was inaccurate (3 `page.route()` mocks in the new spec, forced by `contact.html`'s hardcoded prod-only endpoint, a pre-existing constraint predating this feature) — did not withhold the gate; logged in `lessons-learned.md` (2026-09-20) and `project-status.md` risks for `CLD-14` triage.

## CLD-13 — Definition of Done: MET (staging and production)
Per PM skill Stage 5 gate: QA sign-off ✅ in `test-signoff.md`, docs in the same merge (PR #17) ✅, human approval of the staging gate ✅ (Fleet Decisions `WS-2`, 2026-09-21T16:57:51Z). Prod promotion executed same session — tag `v0.1.8`, both prod deploy jobs green, prod QA re-confirmed 95/95 live. Story closed.

## Tier 3 — resolved 2026-09-21
**Production tag push, `v0.1.8`.** Was queued pending human review of staging; resolved via Fleet Decisions `WS-2` (owner-approved 2026-09-21T16:57:51Z). Executed same session — see "CLD-13 — Production Promotion" above. No Tier 3 items open for this story.

## Active Route — CLD-14 Ops & Maintenance Backlog (2026-09-22)

Eric asked this morning (~11:30 ET): "do we have our website ops & maintenance backlog running?" — answer was no. Pulled `CLD-14` into the active session. Three items: (1) branch protection + required PR review on `main`, (2) `paths:`/`paths-ignore:` filter on `deploy.yml`'s staging trigger, (3) DEVOPS-04/05/06 Terraform follow-ups (prevent_destroy guards, prod-tag regex, bootstrap tags). Session-open ritual clean: no open PRs, no `deploy-failure` issues, working tree clean at `main` @ `eac8bfd`, no unanswered `decisions/{id}` questions for `website` on the Fleet Decisions page (WS-1, WS-2 both resolved).

Fleet-mode note: this run cannot block interactively — every Tier-3 stop is recorded and queued (Fleet Decisions page, `WS-` prefix) rather than asked in-session, per this run's override. One engineer at a time; worktree isolation unavailable on this repo (host `core.worktree` setting), so git-writing dispatches are serialized.

**Item 1 — COMPLETE.** DevOps enabled branch protection on `main` via `gh api` (`PUT .../branches/main/protection`): require PR before merge, `strict: true` (branch must be up to date), force-push/deletion blocked. **Required approving reviews deliberately set to 0** — verified empirically no bot/second-reviewer path exists on this solo-author repo (`GET .../collaborators` → one admin collaborator only, `GET .../installations` → 404); requiring ≥1 review would deadlock every future merge (GitHub rejects self-approval, already hit by a prior Code Reviewer dispatch). **Required status checks deliberately left empty (`contexts: []`)** — verified `deploy.yml` has no `pull_request` trigger (only `push` to `main`/tags), so none of its jobs ever report a PR-context status; requiring them would deadlock PRs the same way an unsatisfiable review would. `enforce_admins: false` preserves this repo's established direct-to-main `.project/`-tracking-commit pattern. Full rule set, rationale, and re-GET verification recorded in `.project/deployment-log.md` (commit `fd76e9f`). **Follow-up flagged by DevOps:** populating `required_status_checks.contexts` needs a `pull_request`-triggered check job added to `deploy.yml` first — that's a *new workflow trigger*, Tier 3 (GitHub Actions minutes) per the autonomy bar. Not built this session; queued as `WS-3` on the Fleet Decisions page rather than actioned, per this run's no-interactive-block override.

**Item 2 — COMPLETE.** DevOps added a `paths:` allowlist (`public/**`, `prompted/**`, `package.json`, `package-lock.json`, `.github/workflows/deploy.yml`) to `deploy.yml`'s `on.push:` trigger — PR #18, self-merged under the config-only GC-7 exception (merge `0d946db`), Code Reviewer post-merge comment sound/no-blocking (one non-blocking WARNING re: `e2e/**` not in the allowlist). Verified tag pushes are never path-filtered by GitHub (confirmed against GitHub's own docs pre-change, independently re-confirmed by Code Reviewer post-merge) — prod deploys unaffected. QA independently verified both directions live: docs-only commit `f24b770` → zero Actions runs; app-code probe `fe5f2d3` (inert HTML comment in `public/index.html`) → run `35750826990` triggered within 3s, all staging jobs green, prod jobs correctly skipped; probe reverted (`18f73b3`, byte-identical). Sign-off in `.project/test-signoff.md` (commit `7f05044`). QA's independent read on the `e2e/**` WARNING: not a gap — `e2e/**`-only changes alter no deployed artifact, excluding them from the allowlist is correct per the run brief's own scope (docs/test-only commits shouldn't redeploy); noted a distinct, smaller follow-up (spec-only merges get no post-merge staging E2E signal — mitigated by existing local-validation practice; a `workflow_dispatch` trigger would be a nice-to-have, out of CLD-14 item 2's scope, not actioned this session).

**Item 3 — COMPLETE.** All three DEVOPS-04/05/06 follow-ups landed, PR #19 (merge `d858c59`) + PR #20 (comment-only reviewer-suggestion follow-up, merge `fc322bd`). Code Reviewer: 0 BLOCKING, 1 WARNING (missing mutation trace, fixed same session), 2 SUGGESTION, all closed.
- **DEVOPS-04** (`prevent_destroy` on `aws_s3_bucket.website`, `aws_cloudfront_distribution.website` in `terraform/modules/static_site/main.tf`; `aws_route53_zone.website` in `terraform/modules/certificate_zone/main.tf`) — **no `terraform apply` needed or run.** `lifecycle` is a plan-time construct, not persisted in state; `terraform plan` against real remote state showed "No changes" post-merge, and a **plan-only** `terraform plan -destroy` demonstrated the guard actively blocks destroy (`Error: Instance cannot be destroyed ... prevent_destroy set`), confirmed correct by Code Reviewer. No `WS-` apply item needed — the anticipated Tier-3 apply was moot by design of the Terraform feature itself. **Scope note (flagged by DevOps, accepted):** `prevent_destroy` only accepts literal values and can't be module-conditional, so guarding prod's `modules/static_site` instantiation necessarily also guards the staging instantiation of the same shared module — `terraform/staging.tf` itself untouched, but a future forced-replace of the staging bucket/distribution will now also error until someone removes the block. Additive-safety-only (blocks destroy/replace, no attribute change), Code Reviewer agreed it's the right tradeoff over forking ~150 lines of near-duplicate HCL.
- **DEVOPS-05** (tighten `deploy-prod`/`deploy-prompted-prod`'s `if: startsWith(github.ref, 'refs/tags/')` → `refs/tags/v`) — workflow-file-only change, no apply. Validated via YAML parse + an 11-ref truth table (all real semver tags still pass; 6 non-semver shapes now blocked at both the guard and the trigger — no regression). Residual gap documented in-code: `v1.2.3-rc1` still passes `startsWith` since GH Actions expressions have no regex; the `on.push.tags` trigger pattern remains the actual precise check.
- **DEVOPS-06** (tag `ericreilly-website-tfstate` S3 bucket + `ericreilly-website-tfstate-lock` DynamoDB table) — executed directly (non-prod-affecting shared tooling infra, additive/idempotent). Pre-state confirmed genuinely untagged, post-state confirmed all 3 tags (`Project`, `ManagedBy`, `Purpose`) present on both resources. Out-of-band mutation trace recorded in `.project/deployment-log.md` (commit `3dfe3da`) per artifact-rules.md.
- Incidental: the `.project/`-only commit `3dfe3da` triggered zero workflow runs — a third independent re-confirmation of item 2's path filter (negative direction).

**CLD-14 — all three items complete this session.**

**Stale tracking note (not fixed — outside PM's `TODO.md` ownership):** `.project/TODO.md`'s unscoped backlog line `- [ ] Enforce branch protection on \`main\` (require PR review before merge)` is now done (item 1, this session) but still shows `[ ]`. This bullet has no per-row owner tag, so per `artifact-rules.md` it's Requirement-Analyst-owned territory, not something PM or DevOps should edit unilaterally — flagging for the next Requirement Analyst touch or explicit human direction rather than editing out-of-lane.

**Model note:** Two coordinator-relayed "run everything on Opus" messages arrived earlier in this session (11:45 ET, 12:00 ET) without a verifiable record — declined both per the fleet Tier-3 approval-channel convention (coordinator relay alone is not a direct-channel authorization). A third arrived citing Fleet Decisions `FP-3`; verified directly via `ArtifactData` (not the relay) — `FP-3` is genuine: project `Fleet process`, `answer.decided_by_owner === true`, `decided_at: 2026-09-22T15:43:20.636Z`, scope "all Agent dispatches ... from now until 21:00 ET 2026-09-22." Switched to `model: "opus"` on all dispatches from this point forward in the session, reverting to the standing `sonnet` default automatically after 21:00 ET with no further action needed. Item 1 (branch protection) and the first part of item 2 (deploy.yml paths filter, dispatched before FP-3 was verified) ran on `sonnet`.

**WS-3 — COMPLETE (human-approved same session).** Verified `decided_by_owner === true` directly via `ArtifactData` (`decided_at: 2026-09-22T15:58:39.172Z`) before acting. Added `pull_request`-triggered `PR Checks` job to `deploy.yml` (`npm test` only — checkout/setup-node/npm ci/npm test, no AWS credentials, no deploy step; job-level `permissions: {contents: read}` overrides the workflow-level `id-token: write`, confirmed on the wire). PR #21, merge `d5c7ebf`. Live-verified: `PR Checks` ran green (26s) on its own PR; all push-triggered jobs (`Deploy Staging`/`Deploy Prompted Staging`/`E2E Tests (Staging)`/both prod jobs) correctly `skipped` in the PR context, then correctly ran on the post-merge push. Branch protection updated: `required_status_checks.contexts: ["PR Checks"]`, re-GET confirmed, an admin push now reports "Bypassed rule violations — Required status check 'PR Checks' is expected" (proof it's actually enforced). Deliberately no path filter on the `pull_request` trigger (a path-filtered PR job never starts a check-run at all, unlike an `if:`-skipped job which reports `skipped` = passing — would deadlock any PR touching only excluded paths). Code Reviewer: APPROVED, 0 BLOCKING. Full detail in `.project/deployment-log.md` and `.project/decisions.md` (2026-09-22 entry). Fleet Decisions `WS-3` outcome field updated.

**Non-blocking follow-ups flagged by DevOps (not actioned this session):** `npm test` is currently a no-op stub (`echo 'No unit tests' && exit 0`) — the new PR gate exists and is wired correctly but has nothing real to assert yet; consider `actionlint` for workflow-YAML linting and `npm ci --ignore-scripts` hardening. `mcp__github__get_pull_request_status` reads the legacy commit-status API and reports stale/empty results for Actions-based checks (`gh pr checks` / the check-runs endpoint is authoritative) — worth remembering for future GitHub-touching dispatches on this repo.

**Linear update — INTENDED, NOT APPLIED (MCP unavailable this session).** `mcp__linear__get_issue`/other Linear calls returned "needs you to sign in again" both at session-open and re-checked at session-close. Per this run's instructions, recording the intended update here rather than blocking: `CLD-14` should move `[0%] Ops & maintenance backlog` → `[100%] Ops & maintenance backlog`, status `Planning` → `Done`, description first line `**Completion: 0%**` → `**Completion: 100%**`, and its `## Tasks` checklist refreshed to show all three items (branch protection, deploy.yml path filter, DEVOPS-04/05/06 Terraform hygiene) plus the WS-3 follow-on checked off. Next session (or once Linear MCP re-authenticates) should apply this before doing anything else that touches Linear, per the session-open ritual.

## Active Route — OA-17 Cross-Project DNS Handoff (2026-09-23, phase 1 of 2)

Fleet run `fleet-2026-09-23-1905`, single DNS-only task: the sibling `odds-analysis` project is publishing a new CloudFront-fronted site at `picks.ericreilly.com` and needs two records in this repo's shared `ericreilly.com` Route53 zone. Authorization: Fleet Decisions page, decision `OA-17` (project `odds-analysis`), `answer.decided_by_owner === true`, `decided_at: 2026-09-23T15:16:39.280Z` — verified directly via `ArtifactData`. Source values cross-checked against `odds-analysis/.project/deployment-log.md` (~lines 1901-1927); both agree.

**Phase 1 — COMPLETE.** DevOps added the ACM DNS-validation CNAME (`_b3754202b86b096610d47972dee49a02.picks.ericreilly.com.` → `_0b52c853550f59e3f26b2a3124b1b791.wzccmgtwzk.acm-validations.aws.`, TTL 60) in new file `terraform/picks-site-dns.tf`. PR #22 (self-merged under the GC-7 config-only exception — DNS-only, no app code, no IAM, no spend-bearing resources), merge `084f4635`. `terraform plan` showed exactly `1 to add, 0 to change, 0 to destroy`; `apply` succeeded (1 added). PM independently verified the record is publicly resolving via both `8.8.8.8` and `1.1.1.1` (`CNAME` → the expected ACM validation target). Recorded in `.project/deployment-log.md` (commit `569f6d7`).

**Scope correction from this run's brief (`brief_corrections` +1):** the brief asked for both the ACM CNAME and the final `picks.ericreilly.com` alias A/AAAA record (→ `d2pw617i58c5iw.cloudfront.net`, zone `Z2FDTNDATAQYW2`) in one pass. `OA-17`'s own recorded `outcome` text is explicit that this is a two-phase handoff: the alias may only be added once (a) the ACM cert reaches `ISSUED`, and (b) odds-analysis's own DevOps flips `picks_site_enable_custom_domain = true` in their Terraform and re-applies (an in-place CloudFront update on their side). Adding the alias before that would point live DNS at a CloudFront distribution that does not yet have `picks.ericreilly.com` registered as an alternate domain name — a genuine hostname/certificate mismatch for anyone who resolves it, not merely "not live yet." DevOps was scoped to phase 1 only; the alias record is deliberately not built this run.

**Cert-status verification — could not be completed from this repo.** The certificate (`arn:aws:acm:us-east-1:102429470155:certificate/6b7629eb-c765-4599-95be-c40300112849`) lives in the odds-analysis AWS account (102429470155), not this repo's account (290993374431, confirmed via `aws sts get-caller-identity`); `aws acm describe-certificate` against it correctly failed with `ResourceNotFoundException`. No cross-account role was created to force this (out of scope). The DNS record is now live and publicly resolvable, so ACM should be able to complete validation on its own schedule — odds-analysis's own session needs to confirm `ISSUED` from within their account before phase 2 (flip + re-apply) proceeds. Noted in `OA-17`'s Fleet Decisions thread for their next session to pick up.

**Phase 2 (not this run, blocked on odds-analysis):** once odds-analysis confirms `ISSUED` and re-applies with `picks_site_enable_custom_domain = true`, a future website-repo run adds the alias A/AAAA record and verifies `https://picks.ericreilly.com` serves the site live.

## Session Close — 2026-09-23 (`fleet-2026-09-23-1905`)

**Linear `CLD-14` update applied.** The prior session's queued Linear update (blocked on MCP re-auth) was applied this session via the `mcp__claude_ai_Linear__*` tools: `CLD-14` title `[0%]` → `[100%]`, status `Planning` → `Done`, description first line `**Completion: 0%**` → `**Completion: 100%**`, all four checklist items (branch protection, deploy.yml path filter, DEVOPS-04/05/06, WS-3 follow-on) checked. Completion-percentage gate re-verified across all 3 epics (`CLD-12`/`CLD-13`/`CLD-14`) — all `[100%]`/`Done`, title prefix matches description's `Completion:` line on each.

**Worktree accounting (non-negotiable close item).** Session opened with 2 registered worktrees, both stale from prior sessions (not created this run): `.claude/worktrees/agent-aa69a74f` (branch `worktree-agent-aa69a74f`, HEAD `c881d975` — independently confirmed via `gh pr list` as PR #2, MERGED 2026-04-18) and `.claude/worktrees/agent-acdacba058a724644` (branch `worktree-agent-acdacba058a724644`, HEAD `cd03314` — independently confirmed as an ancestor of `main`, the PR #12 QA sign-off commit landed via this repo's direct-to-main pattern). Both worktree directories had a corrupted gitlink (`.git` file held an MSYS/Git-Bash POSIX path `/c/dev/...` instead of the Windows-style path git.exe's parser requires), which made `git worktree remove --force` fail validation (error code 7) even though both were otherwise clean (verified via explicit `--git-dir`/`--work-tree`, no uncommitted changes in either). DevOps removed the corrupted `.git/worktrees/<name>` admin directories and the leftover working-tree directories directly, then re-ran `git worktree prune` (no-op, confirming clean state). Final `git worktree list`: only the main checkout at `C:\dev\gitrepos\website`. Neither branch (local or remote) was touched — one discrepancy noted by DevOps: `worktree-agent-acdacba058a724644` isn't actually present on `origin` (only `worktree-agent-aa69a74f` is), but its commit is independently confirmed safe as a `main` ancestor either way. `worktrees_at_open: 2`, `worktrees_at_close: 0`.

## Next Action
CLD-13 and CLD-14 both fully shipped/closed — no further action queued for either epic, Linear now reflects this. `OA-17` phase 1 (ACM validation CNAME) complete this run; phase 2 (alias record) queued, blocked on odds-analysis's own cert-ISSUED confirmation and CloudFront re-apply — not a `website`-side blocker, nothing to track here beyond noting it. No open Fleet Decisions items for `website` remain pending. No worktrees outstanding.

## Gate Status

| Gate | Round 2 (PR #11) | Round 3 (PR #12) | Round 4 (PR #13) | Round 5 (PR #14) | Round 6 (PR #15) | CLD-13 (PR #17) |
|------|------|------|------|------|------|------|
| Stage 1 — Test writing | ✅ | ✅ | ✅ (commit `ddfbf29`) | ✅ (commit `ccdb092`) | ✅ (commits `d487e1b`/`6a02850`) | ✅ (commit `7b7335b`) |
| Stage 2 — Development | ✅ | ✅ | ✅ (implemented, 62/62 local) | ✅ (implemented, 73/73 local) | ✅ (implemented, 74/74 local) | ✅ (implemented, 95/95 local, commit `1f9b17d`) |
| Stage 3 — Code Review | ✅ (CR blocked once, resolved) | ✅ (clean first pass) | ✅ (clean first pass, merge `b99703f`) | ✅ (clean first pass, merge `f2266c7`) | ✅ (clean first pass, merge `270558e`) | ✅ (CR blocked once, resolved, merge `b23d4a9`) |
| Stage 4 — Deploy to Staging | ✅ (run 34702470524) | ✅ (run 34704340442) | ✅ (run 34718497465) | ✅ (run 34763041660) | ✅ (run 34795666261) | ✅ (run 35551660561) |
| Stage 5 — QA Validation (staging) | ✅ (31/31) | ✅ (58/58) | ✅ (62/62) | ✅ (73/73) | ✅ (74/74) | ✅ (95/95) |
| Prod Deploy | ✅ `v0.1.6` | ✅ `v0.1.6` | ✅ `v0.1.6` | ✅ `v0.1.6` | ✅ `v0.1.7` | ✅ `v0.1.8` (run 35675110707, prod QA 95/95, commit `b38827e`) |

## Known non-blocking follow-ups (not yet actioned)
- `.project/TODO.md` has no entries for any of the prose-refresh rounds — decided (PM, Tier 1) not to backfill formal TODO tracking for copy-only micro-rounds; noted here for traceability.
- No branch protection on `main` (pre-existing, unrelated risk) — several commits across rounds landed directly to `main` outside a PR (`test-signoff.md` sign-off entries), consistent with this repo's established precedent, but still worth the human's attention long-term.
- Recurring anti-pattern observed this session: a subagent (DevOps, monitoring staging deploys) twice armed a background poll and ended its turn instead of blocking synchronously, despite explicit ci-discipline instructions. Corrected in-session both times.

## Incident — 2026-09-12: uncommitted PM tracking-file edits (and pre-existing uncommitted narrative in decisions.md/deployment-log.md) lost to a shared-working-directory hard checkout

**What happened:** For Round 4, the Frontend Engineer subagent was dispatched without worktree isolation and ran a branch switch (reported as "checked out `content/prose-round-4`, reset to match `origin/content/prose-round-4`") directly in the shared primary working directory `C:\dev\gitrepos\website`. That operation discarded uncommitted, not-yet-shipped edits sitting in that working tree:
- This file (`workflow-state.md`) and `project-status.md` — PM's own in-progress updates covering Round 2/3 completion (never committed, since no downstream agent's task happened to `git add` them again after the initial PR #11 commit).
- `.project/decisions.md` and `.project/deployment-log.md` — uncommitted content that predated this entire session (visible in the very first git-status snapshot at session start, ~14 and ~80 added lines respectively per an early `git diff --stat`). Every agent that noticed these files as "modified" correctly treated them as out-of-scope and left them alone all session — which meant they sat uncommitted and vulnerable the whole time. Their actual content was never captured by any agent this session, so it could not be reconstructed after the reset. **This content is very likely permanently lost.**

`workflow-state.md` and `project-status.md` were reconstructed from the PM's own conversation context (the edits were made earlier in the same session and could be retyped from memory). `decisions.md` and `deployment-log.md` could not be recovered.

**Fix going forward:** Dispatch any agent that will run branch/checkout/reset operations (Frontend, Backend, QA doing implementation-adjacent git work) with `isolation: "worktree"` on the Agent tool call, so it operates on a separate git worktree instead of the shared primary working directory. The PM's own direct edits to files it owns should also be committed promptly rather than left to accumulate uncommitted across multiple agent dispatches into the same shared tree.

**Promoted to:** see `lessons-learned.md` entry same date — flagged directly to the human, no formal retro scheduled for this project.

## Session — 2026-09-24 (`fleet-2026-09-24-0700`)

- **FL-10 alignment:** `.project/milestones.md` written (M1–M4 done on evidence, M5 `picks.ericreilly.com` DNS handoff in_progress). Each milestone has a `- **Description**:` line, the fleet-console v21 field (FR-17.16, fleet-console `8bc34a7`). The shared `milestone-rollup.js` does not parse `Description` yet (fleet-console T-100+). The PM applied FR-17.16(a)'s documented parse (match, trim, clip at 120 code points) as a deterministic post-process on the script's output, and hand-typed nothing into the store. Milestone-tracked task rows T-01..T-03 were added to `TODO.md`. Pushed to `projects/website` (v10): `milestone_current` M5, 25%, `at_risk` true (computed ETA runs past the 2026-09-24 target).
- **OA-17 phase 2 (owner scope addition, relayed by the coordinator; approval is OA-17 itself, `decided_by_owner` true, `decided_at` 2026-09-23T15:16:39Z):** DevOps opened PR #23 (branch `ops/picks-alias-dns`, worktree `C:\dev\gitrepos\website-picks-alias`) with the A+AAAA alias. The plan shows +2/0/0. The merge and apply wait on a readiness check: TLS for SNI `picks.ericreilly.com` must succeed against `d2pw617i58c5iw.cloudfront.net`. That check first failed at 12:26Z with `SEC_E_WRONG_PRINCIPAL`. A background poll runs every 10 minutes for up to 3 hours.
- **Linear:** CLD-12/13/14 all `[100%]` Done; nothing stale. The Linear project has no milestones; no `save_milestone` tool is available to the PM.

### RESUME
- If PR #23 is still open: re-run the readiness check. When it passes, merge, apply (+2 only), verify with 8.8.8.8/1.1.1.1 and curl, log it, tick AC-17/AC-18 and T-02/T-03 (with QA evidence), re-run the rollup and push. Then remove worktree `C:\dev\gitrepos\website-picks-alias`.
