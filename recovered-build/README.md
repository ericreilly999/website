This folder preserves the exact deployed site that was downloaded from `s3://ericreilly.com-prod` on 2026-03-23.

The source of truth for ongoing development is the recovered React app in `src/` and `public/`.
